{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "gnucobol - compile &",
            "type": "shell",
            "command": "./bin/buildAndRun.sh",
            "args": [
                "${workspaceFolder}",
                "${file}"
            ],
            "presentation": {
                "reveal" : "always",
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
